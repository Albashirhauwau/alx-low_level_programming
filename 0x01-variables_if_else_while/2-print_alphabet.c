#include <stdio.h>

/**
 * main -  alphabet in lowercase
 *
 * Return: Always return 0 (success)
 *
 */

int main(void)
{
	int ch ='a';

		while (ch <= 'z');
		{
			putchar(ch);
			ch++;
		}
			putchar('\n');
			return (0);
}
		
